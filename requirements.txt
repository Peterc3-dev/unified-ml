# Runtime dependencies for the pure-Python parts of unified-ml.
#
# Only numpy is required for the always-importable code path
# (src/gguf_loader.py and the CPU path of vulkan/benchmark_comparison.py).
numpy>=1.24

# --- Optional, hardware/backend-specific (NOT installed by default) ---
#
# The benchmark scripts import these lazily and degrade gracefully when
# they are absent, so they are intentionally left out of the pinned set:
#
#   torch        # benchmarks/pytorch_comparison.py — install the ROCm/CUDA
#                # build that matches your GPU, e.g.
#                #   pip install torch --index-url \
#                #     https://download.pytorch.org/whl/rocm6.2
#   kp (kompute) # vulkan/benchmark_comparison.py — Vulkan compute backend
